<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljf.sidanfinance.dao.mapper.EmployeeMapper">
  <resultMap id="BaseResultMap" type="com.ljf.sidanfinance.dao.model.Employee">
    <!--
      WARNING - @mbg.generated
    -->
    <constructor>
      <idArg column="id" javaType="java.lang.Integer" jdbcType="INTEGER" />
      <arg column="name" javaType="java.lang.String" jdbcType="VARCHAR" />
      <arg column="type" javaType="java.lang.Integer" jdbcType="INTEGER" />
      <arg column="date_insert" javaType="java.util.Date" jdbcType="TIMESTAMP" />
      <arg column="date_update" javaType="java.util.Date" jdbcType="TIMESTAMP" />
      <arg column="delete_mark" javaType="java.lang.Integer" jdbcType="INTEGER" />
    </constructor>
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
    -->
    id, name, type, date_insert, date_update, delete_mark
  </sql>

  <select id="getList" resultType="java.util.HashMap">
    SELECT id AS id,name AS name,CASE type WHEN 0 THEN '管理'
                                           WHEN 1 THEN '木工'
                                           WHEN 2 THEN '瓦工'
                                           WHEN 3 THEN '油工'
                                           WHEN 4 THEN '电工'
                                           WHEN 5 THEN '小工'
                                           WHEN 6 THEN '抹灰工'
                                           END AS typeName,
           type AS type,DATE_FORMAT(date_insert,'%Y-%m-%d %H:%i:%s') AS dateInsert
    FROM employee
    WHERE delete_mark = 0
    <if test="params.name != null and params.name != ''">
        AND name like CONCAT('%',#{params.name},'%')
    </if>
    <if test="params.type != null and params.type != ''">
        AND type = #{type}
    </if>
    ORDER BY date_insert DESC
  </select>

</mapper>